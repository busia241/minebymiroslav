# 🚀 Деплой через GitHub + Vercel

## Шаг 1: Создание репозитория на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите **"New repository"** (зеленая кнопка справа вверху)
3. Заполните:
   - **Repository name**: `minedrop-site` (или любое другое имя)
   - **Description**: `Minedrop game site with access management`
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
4. Нажмите **"Create repository"**

## Шаг 2: Загрузка файлов в GitHub

### Вариант A: Через GitHub веб-интерфейс (самый простой)

1. На странице созданного репозитория нажмите **"uploading an existing file"**
2. Перетащите ВСЕ файлы из папки `minedrop-site` в браузер
3. Нажмите **"Commit changes"** внизу страницы

### Вариант B: Через Git (если установлен)

Откройте терминал в папке `minedrop-site` и выполните:

```bash
git init
git add .
git commit -m "Initial commit - Minedrop site"
git branch -M main
git remote add origin https://github.com/ВАШ-USERNAME/minedrop-site.git
git push -u origin main
```

**Замените `ВАШ-USERNAME` на ваш GitHub username!**

## Шаг 3: Деплой на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"** и войдите через **GitHub** (если еще не зарегистрированы)
3. После входа нажмите **"Add New..."** → **"Project"**
4. Найдите ваш репозиторий `minedrop-site` в списке
5. Нажмите **"Import"**
6. Vercel автоматически определит настройки:
   - **Framework Preset**: Other (или оставьте как есть)
   - **Root Directory**: `./` (оставьте как есть)
   - **Build Command**: оставьте пустым
   - **Output Directory**: оставьте пустым
7. Нажмите **"Deploy"**
8. Подождите 1-2 минуты пока идет деплой

## Шаг 4: Получение URL

После успешного деплоя вы увидите:
- ✅ **Success!** 
- Ваш URL будет вида: `https://minedrop-site-xxxxx.vercel.app`

**Скопируйте этот URL!** Он понадобится для обновления расширения.

## Шаг 5: Обновление расширения

После получения URL от Vercel, обновите расширение `by miroslav`:

### 1. Откройте `by miroslav/manifest.json`

Найдите и замените `cv749818.tw1.ru` на ваш Vercel URL:

```json
"host_permissions": [
  "*://ваш-проект-xxxxx.vercel.app/*",
  "*://*.stake-engine.com/*",
  "*://stake1061.com/*"
],
"externally_connectable": {
  "matches": ["*://ваш-проект-xxxxx.vercel.app/*"]
}
```

И в `content_scripts`:

```json
{
  "matches": ["*://ваш-проект-xxxxx.vercel.app/*"],
  "js": ["content.js"],
  "run_at": "document_start"
}
```

### 2. Откройте `by miroslav/background.js`

Замените первую строку:

```javascript
const SERVER_HOST = "https://ваш-проект-xxxxx.vercel.app";
```

### 3. Перезагрузите расширение

1. Откройте `chrome://extensions/`
2. Найдите "Minedrop Controller by miroslav"
3. Нажмите кнопку обновления (🔄)

## ✅ Готово!

Теперь можно использовать:
- `https://ваш-проект-xxxxx.vercel.app/minedrop` - страница входа
- `https://ваш-проект-xxxxx.vercel.app/minedrop/admin` - админ панель
- `https://ваш-проект-xxxxx.vercel.app/minedrop/console` - консоль игры

## 🔄 Обновление сайта

После любых изменений в коде:

1. Загрузите обновленные файлы в GitHub (через веб-интерфейс или `git push`)
2. Vercel автоматически задеплоит новую версию (обычно за 1-2 минуты)

## 📝 Примечания

- Vercel дает бесплатный домен `.vercel.app`
- Можно подключить свой домен в настройках проекта Vercel
- Все изменения автоматически деплоятся при обновлении GitHub репозитория
- Код доступа по умолчанию: `1234567890123456`

