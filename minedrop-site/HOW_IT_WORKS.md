# 🔄 Как это работает

## Архитектура системы

```
Пользователь → Расширение → Ваш сайт (Vercel) → Игра (stake-engine.com)
```

## Процесс работы

### 1. Выдача доступа (Админ панель)

1. Откройте `/minedrop/admin`
2. Создайте новый токен:
   - Введите имя пользователя (например: "Admin")
   - Введите код доступа (16 цифр, например: "1234567890123456")
   - Нажмите "Создать токен"
3. Система создаст уникальный токен и свяжет его с кодом доступа

### 2. Вход пользователя

1. Пользователь открывает `/minedrop`
2. Вводит код доступа (16 цифр)
3. Система проверяет код через `/api/tokens/get?accessCode=XXX`
4. Если код валиден, пользователь перенаправляется на `/minedrop/console`

### 3. Загрузка игры

1. На странице `/minedrop/console`:
   - Загружается iframe с игрой (`paperclip.live.stake-engine.com`)
   - JavaScript получает токен из API по коду доступа
   - Токен отправляется расширению через `postMessage`

### 4. Работа расширения

1. Расширение получает токен через `content.js`
2. Сохраняет токен в `chrome.storage`
3. Когда игра делает запросы к API:
   - `interceptor.js` перехватывает `fetch` запросы
   - `content.js` отправляет запросы в расширение
   - `background.js` проксирует запросы через `/wallet/*` на ваш сервер

### 5. Проксирование запросов

1. Запросы от игры идут на `rgs.stake-engine.com`
2. Расширение перехватывает их и отправляет на ваш сервер `/wallet/*`
3. Ваш сервер (`api/wallet/[...path].js`) может:
   - Модифицировать запросы
   - Добавлять бонусы
   - Изменять баланс
   - И т.д.

### 6. Отзыв доступа

1. В админ панели нажмите "Отозвать" рядом с токеном или кодом
2. Токен/код помечается как неактивный
3. Пользователь больше не сможет войти с этим кодом

## Поток данных

```
┌─────────────┐
│  Пользователь │
└──────┬───────┘
       │
       ▼
┌─────────────────┐      ┌──────────────┐
│  Ваш сайт       │◄─────►│  Админ панель │
│  (Vercel)       │      └──────────────┘
└──────┬──────────┘
       │
       │ postMessage
       ▼
┌─────────────────┐
│   Расширение    │
│  (Chrome)       │
└──────┬──────────┘
       │
       │ Проксирование
       ▼
┌─────────────────┐      ┌──────────────────┐
│  Ваш сервер     │─────►│  Игра            │
│  /wallet/*      │      │  stake-engine.com│
└─────────────────┘      └──────────────────┘
```

## Ключевые компоненты

### Frontend (public/)
- `index.html` - Страница входа
- `console.html` - Страница с игрой
- `admin.html` - Админ панель

### Backend API (api/)
- `tokens/*` - Управление токенами
- `wallet/*` - Проксирование запросов игры
- `script/*` - Скрипт для инжекции
- `status.js` - Проверка статуса
- `exit.js` - Выход

### Расширение (by miroslav/)
- `manifest.json` - Конфигурация
- `background.js` - Сервисный воркер (проксирование)
- `content.js` - Скрипт контента (перехват сообщений)
- `interceptor.js` - Перехват fetch запросов

## Важные моменты

1. **Токены хранятся в памяти** - при перезапуске Vercel функции они теряются
2. **Код доступа по умолчанию**: `1234567890123456`
3. **Все запросы проксируются** через `/wallet/*` для возможности модификации
4. **Расширение работает автоматически** после установки и настройки домена

## Модификация запросов для бонусов

В файле `api/wallet/[...path].js` можно добавить логику:

```javascript
// Пример: увеличение баланса
if (endpoint.includes('balance')) {
    const responseData = JSON.parse(responseData);
    responseData.balance = responseData.balance * 1.1; // +10%
    responseData = JSON.stringify(responseData);
}
```

