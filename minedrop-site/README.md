# Minedrop Site

Веб-сайт для игры Minedrop с системой управления доступами, развернутый на Vercel.

## 🎯 Возможности

- ✅ Система выдачи и отзыва токенов доступа
- ✅ Проксирование запросов к игре через `/wallet/*`
- ✅ Админ панель для управления доступами
- ✅ Динамическая загрузка токенов для расширения
- ✅ Автоматическая проверка статуса сессии

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Локальная разработка

```bash
npm run dev
```

### 3. Деплой на Vercel

#### Вариант 1: Через GitHub (рекомендуется)

1. Создайте репозиторий на GitHub
2. Загрузите код:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ваш-username/minedrop-site.git
   git push -u origin main
   ```
3. Зайдите на [vercel.com](https://vercel.com)
4. Импортируйте репозиторий → Deploy

#### Вариант 2: Через Vercel CLI

```bash
npm i -g vercel
vercel
```

## 📁 Структура проекта

```
minedrop-site/
├── public/
│   ├── index.html          # Страница входа с кодом доступа
│   ├── console.html        # Страница с игрой (iframe)
│   └── admin.html          # Админ панель управления
├── api/
│   ├── status.js           # API проверки статуса
│   ├── exit.js             # API выхода
│   ├── tokens/
│   │   ├── create.js       # Создание токена
│   │   ├── revoke.js       # Отзыв токена
│   │   ├── list.js         # Список токенов
│   │   └── get.js          # Получение токена по коду
│   ├── wallet/
│   │   └── [...path].js    # Проксирование запросов /wallet/*
│   └── script/
│       └── get_script.php.js # Скрипт для инжекции в игру
├── lib/
│   └── tokens.js           # Логика управления токенами
├── vercel.json             # Конфигурация Vercel
├── package.json
└── README.md
```

## 🔧 API Endpoints

### Управление токенами

- `POST /api/tokens/create` - Создать новый токен
  ```json
  {
    "username": "Admin",
    "accessCode": "1234567890123456"
  }
  ```

- `POST /api/tokens/revoke` - Отозвать токен
  ```json
  {
    "tokenId": "token_123" // или
    "accessCode": "1234567890123456"
  }
  ```

- `GET /api/tokens/list` - Список всех токенов и кодов
- `GET /api/tokens/get?accessCode=XXX` - Получить токен по коду

### Проксирование

- `POST /wallet/*` - Проксирование запросов к игре
- `GET /api/status` - Проверка статуса сессии
- `GET /api/script/get_script.php` - Скрипт для инжекции

## 🌐 URL после деплоя

- `https://ваш-проект.vercel.app/minedrop` - Страница входа
- `https://ваш-проект.vercel.app/minedrop/console` - Консоль игры
- `https://ваш-проект.vercel.app/minedrop/admin` - Админ панель

## 🔗 Обновление расширения

После деплоя обновите расширение `by miroslav`:

### `manifest.json`:
```json
"host_permissions": [
  "*://ваш-проект.vercel.app/*",
  "*://*.stake-engine.com/*"
],
"externally_connectable": {
  "matches": ["*://ваш-проект.vercel.app/*"]
}
```

### `background.js`:
```javascript
const SERVER_HOST = "https://ваш-проект.vercel.app";
```

## 📝 Использование админ панели

1. Откройте `/minedrop/admin`
2. Введите имя пользователя и код доступа (16 цифр)
3. Нажмите "Создать токен"
4. Используйте код доступа для входа на `/minedrop`
5. При необходимости отзовите токен через админ панель

## ⚠️ Важные замечания

- Токены хранятся в памяти сервера (при перезапуске теряются)
- Для продакшена рекомендуется использовать Vercel KV или базу данных
- Код доступа по умолчанию: `1234567890123456`
- Все запросы к игре проксируются через `/wallet/*` для возможности модификации

## 🔒 Безопасность

- Добавьте защиту админ панели (например, через переменные окружения)
- Используйте HTTPS (автоматически на Vercel)
- Регулярно меняйте коды доступа
- Ограничьте доступ к API endpoints
