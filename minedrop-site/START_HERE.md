# 🚀 Начни отсюда!

## Простая инструкция для деплоя

### 1️⃣ Создай репозиторий на GitHub

1. Зайди на [github.com](https://github.com) и войди в аккаунт
2. Нажми **"New repository"** (зеленая кнопка)
3. Название: `minedrop-site`
4. Выбери **Public** или **Private**
5. **НЕ** ставь галочки на README, .gitignore, license
6. Нажми **"Create repository"**

### 2️⃣ Загрузи файлы в GitHub

**Самый простой способ:**

1. На странице репозитория нажми **"uploading an existing file"**
2. Перетащи ВСЮ папку `minedrop-site` (со всеми файлами внутри) в браузер
3. Нажми **"Commit changes"** внизу

**Или через терминал (если Git установлен):**

```bash
cd minedrop-site
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ТВОЙ-USERNAME/minedrop-site.git
git push -u origin main
```

### 3️⃣ Задеплой на Vercel

1. Зайди на [vercel.com](https://vercel.com)
2. Войди через **GitHub** (если еще не зарегистрирован)
3. Нажми **"Add New..."** → **"Project"**
4. Выбери репозиторий `minedrop-site`
5. Нажми **"Import"**
6. Оставь все настройки как есть
7. Нажми **"Deploy"**
8. Подожди 1-2 минуты

### 4️⃣ Скопируй URL

После деплоя получишь URL вида:
```
https://minedrop-site-xxxxx.vercel.app
```

**Сохрани этот URL!**

### 5️⃣ Обнови расширение

Открой файлы расширения `by miroslav`:

**В `manifest.json`** замени `cv749818.tw1.ru` на твой Vercel URL:
```json
"host_permissions": ["*://твой-проект.vercel.app/*", ...]
```

**В `background.js`** замени первую строку:
```javascript
const SERVER_HOST = "https://твой-проект.vercel.app";
```

Перезагрузи расширение в `chrome://extensions/`

### ✅ Готово!

Открой:
- `https://твой-проект.vercel.app/minedrop` - вход
- `https://твой-проект.vercel.app/minedrop/admin` - админка

**Код доступа по умолчанию:** `1234567890123456`

---

📖 Подробная инструкция: [GITHUB_DEPLOY.md](./GITHUB_DEPLOY.md)

