# 🚀 Инструкция по деплою на Vercel

## Шаг 1: Подготовка GitHub репозитория

1. Создайте новый репозиторий на GitHub:
   - Зайдите на [github.com](https://github.com)
   - Нажмите "New repository"
   - Назовите репозиторий (например: `minedrop-site`)
   - Выберите "Public" или "Private"
   - **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)

2. Загрузите код в репозиторий:

```bash
cd minedrop-site
git init
git add .
git commit -m "Initial commit - Minedrop site"
git branch -M main
git remote add origin https://github.com/ВАШ-USERNAME/minedrop-site.git
git push -u origin main
```

## Шаг 2: Деплой на Vercel

### Вариант A: Через веб-интерфейс (рекомендуется)

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub аккаунт
3. Нажмите **"Add New..."** → **"Project"**
4. Выберите ваш репозиторий `minedrop-site`
5. Vercel автоматически определит настройки:
   - **Framework Preset:** Other
   - **Root Directory:** `./` (оставьте как есть)
   - **Build Command:** оставьте пустым
   - **Output Directory:** оставьте пустым
6. Нажмите **"Deploy"**
7. Дождитесь завершения деплоя (обычно 1-2 минуты)

### Вариант B: Через Vercel CLI

```bash
npm install -g vercel
cd minedrop-site
vercel
```

Следуйте инструкциям в терминале.

## Шаг 3: Получение URL

После деплоя вы получите URL вида:
- `https://minedrop-site-xxxxx.vercel.app`
- Или кастомный URL если настроили домен

**Сохраните этот URL!** Он понадобится для обновления расширения.

## Шаг 4: Обновление расширения

После деплоя нужно обновить расширение чтобы оно работало с новым доменом.

### 4.1 Обновить manifest.json

Откройте `by miroslav/manifest.json` и замените:

```json
"host_permissions": [
  "*://ваш-проект.vercel.app/*",
  "*://*.stake-engine.com/*",
  "*://stake1061.com/*"
],
"externally_connectable": {
  "matches": ["*://ваш-проект.vercel.app/*"]
}
```

И в `content_scripts`:

```json
{
  "matches": ["*://ваш-проект.vercel.app/*"],
  "js": ["content.js"],
  "run_at": "document_start"
}
```

### 4.2 Обновить background.js

Откройте `by miroslav/background.js` и замените первую строку:

```javascript
const SERVER_HOST = "https://ваш-проект.vercel.app";
```

### 4.3 Перезагрузить расширение

1. Откройте `chrome://extensions/`
2. Найдите "Minedrop Controller by miroslav"
3. Нажмите кнопку обновления (🔄)
4. Или удалите и загрузите заново

## Шаг 5: Настройка кода доступа

Откройте `public/index.html` и измените массив `validCodes`:

```javascript
const validCodes = ['ваш-секретный-код'];
```

Или используйте код по умолчанию: `1234567890123456`

## ✅ Проверка работы

1. Откройте `https://ваш-проект.vercel.app/minedrop`
2. Введите код доступа
3. Должна открыться страница с игрой
4. Расширение должно автоматически получить токен

## 🔄 Обновление сайта

После изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Vercel автоматически задеплоит новую версию!

## 📝 Примечания

- Vercel дает бесплатный домен `.vercel.app`
- Можно подключить свой домен в настройках проекта Vercel
- Все изменения автоматически деплоятся при push в GitHub
- API endpoints доступны по адресу `/api/status` и `/api/exit`

